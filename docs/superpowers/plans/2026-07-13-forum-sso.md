# Forum-SSO (Board-Login) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** FriesenSpy per Admin-Schalter hinter den phpBB-Forum-Login stellen (Zugangsschranke + Identifikation über VATSIM-CID + Admin = Forum-Gruppe „Events"), ohne das Forum zu verändern.

**Architecture:** Redirect-SSO. Eine dormante Bridge-Datei `sso.php` liegt (durch den Nutzer/Micha kopiert) neben phpBB, liest die echte Forum-Session und liefert per Redirect ein HMAC-signiertes Kurz-Token an FriesenSpy. FriesenSpy verifiziert es, legt eine **eigene** kurzlebige Session (signiertes Cookie) an und schaltet über eine Gate-Middleware die App frei. Ein Admin-Schalter (`forum_login_enabled`, Default AUS) aktiviert das Gate; das bestehende Admin-Passwort bleibt als Break-glass erhalten.

**Tech Stack:** Python 3.11 / FastAPI / SQLite (`app_settings`), Vanilla-JS-SPA; Bridge-Datei in PHP 8 (phpBB 3.3 public API). Nur Standardbibliothek, keine neue Abhängigkeit.

## Global Constraints

- **Forum wird NICHT angefasst.** Dieser Plan deployt `sso.php` NICHT auf das Forum — er erzeugt die Datei nur im Repo (`deploy/forum/`) zum Kopieren durch den Nutzer/Micha. Kein SSH-Write, kein phpBB-Patch, keine Extension.
- **Default AUS:** `forum_login_enabled` ist ohne Eintrag `"0"`. Ohne konfigurierte Secrets (`SSO_SECRET`/`FORUM_SSO_URL`/`FORUM_SSO_CALLBACK`) ist der Board-Login inaktiv, egal was der Schalter sagt.
- **Break-glass:** Ein gültiges Admin-Cookie (bestehendes `ADMIN_PASSWORD`) zählt IMMER als eingeloggt — auch bei aktivem Gate und bei Forum-Ausfall.
- **Secrets nie in git:** `SSO_SECRET` in FriesenSpys `config.env`; auf Forum-Seite in `sso_config.php`. Repo enthält nur `*.sample`.
- **Admin-Gruppe:** phpBB-Gruppe „Events" = `g=8` → `is_admin`. **CID-Profilfeld:** Anzeigename „VatSim-ID" (interner `field_ident` in `sso.php` konfigurierbar, Default `pf_vatsim_id`).
- **Blau-Regel (UI):** neue klickbare Elemente (Abmelden-Link) nutzen die Klickbar-Farbe; reine Anzeige-Texte bleiben neutral (siehe CLAUDE.md UI-Standards).
- **Versionierung:** Release = Minor-Bump auf **v8.23.0**, CHANGELOG.json oben, Git-Tag `v8.23.0`.

## File Structure

- `app/forum_sso.py` — **neu.** Reine Token-Primitiven: eingehendes Bridge-Token verifizieren (`SSO_SECRET`), eigenes Session-Cookie erstellen/prüfen (`SECRET_KEY`). Keine FastAPI-Abhängigkeit.
- `app/config.py` — **modifiziert.** Neue Settings `SSO_SECRET`, `FORUM_SSO_URL`, `FORUM_SSO_CALLBACK`, `USER_SESSION_MAX_AGE_SEC`.
- `app/main.py` — **modifiziert.** Nonce-Store, Gate-Aktiv-Helfer (+Cache), Admin-Toggle-Endpoints, `/auth/forum/{login,callback,logout}`, `/api/me`, Gate-Middleware.
- `app/static/admin.html` — **modifiziert.** Panel „Board-Login" (Schalter).
- `app/static/index.html` — **modifiziert.** Abmelden-Link + Anzeige des eingeloggten Namens (aus `/api/me`).
- `deploy/forum/sso.php` — **neu.** Fertige Bridge-Datei zum Kopieren (nicht von FriesenSpy ausgeführt).
- `deploy/forum/sso_config.sample.php` — **neu.** Vorlage für `SSO_SECRET` + Feld-Ident.
- `deploy/forum/README.md` — **neu.** Kopier-/Aktivierungs-Anleitung für den Forum-Admin.
- `tests/test_forum_sso.py` — **neu.** Unit-Tests der Primitiven.
- `tests/test_forum_sso_api.py` — **neu.** Endpoint- + Gate-Tests (TestClient).
- `README.md`, `docs/api.md`, `docs/architecture.md`, `config.env`-Beispiel, `app/CHANGELOG.json` — **modifiziert.**

---

### Task 1: Token-Primitiven (`app/forum_sso.py`)

**Files:**
- Create: `app/forum_sso.py`
- Test: `tests/test_forum_sso.py`

**Interfaces:**
- Produces:
  - `USER_COOKIE = "fs_user"`, `SSO_TOKEN_MAX_AGE_SEC = 60`
  - `verify_sso_token(token: str, sso_secret: str, now: float | None = None) -> dict | None`
  - `make_user_token(secret_key: str, sub, name: str, cid: str, is_admin: bool, exp: float) -> str`
  - `verify_user_token(token: str, secret_key: str, now: float | None = None) -> dict | None`

- [ ] **Step 1: Write the failing tests**

Create `tests/test_forum_sso.py`:

```python
"""Unit-Tests der Forum-SSO-Token-Primitiven (kein FastAPI, kein DB)."""
import base64
import hashlib
import hmac
import json

from app import forum_sso

SSO = "shared-forum-secret"
KEY = "friesenspy-secret-key"


def _incoming(claims: dict, secret: str = SSO) -> str:
    """Mint ein eingehendes Bridge-Token genau so, wie sso.php es baut."""
    raw = json.dumps(claims, separators=(",", ":"), sort_keys=True).encode()
    p = base64.urlsafe_b64encode(raw).rstrip(b"=").decode()
    sig = hmac.new(secret.encode(), p.encode(), hashlib.sha256).hexdigest()
    return p + "." + sig


def test_verify_sso_token_accepts_fresh():
    tok = _incoming({"sub": 74, "name": "Tobias", "cid": "1401925",
                     "is_admin": True, "iat": 990, "nonce": "abc"})
    claims = forum_sso.verify_sso_token(tok, SSO, now=1000.0)
    assert claims is not None
    assert claims["cid"] == "1401925"
    assert claims["is_admin"] is True
    assert claims["nonce"] == "abc"


def test_verify_sso_token_rejects_bad_signature():
    tok = _incoming({"iat": 1000, "nonce": "x"}, secret="wrong-secret")
    assert forum_sso.verify_sso_token(tok, SSO, now=1000.0) is None


def test_verify_sso_token_rejects_stale():
    tok = _incoming({"iat": 900, "nonce": "x"})  # 100 s alt > 60 s
    assert forum_sso.verify_sso_token(tok, SSO, now=1000.0) is None


def test_verify_sso_token_requires_nonce():
    tok = _incoming({"iat": 1000})
    assert forum_sso.verify_sso_token(tok, SSO, now=1000.0) is None


def test_verify_sso_token_rejects_garbage():
    assert forum_sso.verify_sso_token("not-a-token", SSO, now=1000.0) is None
    assert forum_sso.verify_sso_token("", SSO, now=1000.0) is None


def test_user_token_roundtrip():
    tok = forum_sso.make_user_token(KEY, 74, "Tobias", "1401925", True, exp=2000.0)
    claims = forum_sso.verify_user_token(tok, KEY, now=1999.0)
    assert claims is not None
    assert claims["sub"] == 74
    assert claims["cid"] == "1401925"
    assert claims["is_admin"] is True


def test_user_token_expired():
    tok = forum_sso.make_user_token(KEY, 74, "T", "1", False, exp=1000.0)
    assert forum_sso.verify_user_token(tok, KEY, now=1000.0) is None


def test_user_token_wrong_key():
    tok = forum_sso.make_user_token(KEY, 74, "T", "1", False, exp=5000.0)
    assert forum_sso.verify_user_token(tok, "other-key", now=1000.0) is None
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/test_forum_sso.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.forum_sso'`

- [ ] **Step 3: Write the implementation**

Create `app/forum_sso.py`:

```python
"""Forum-SSO (Board-Login) für FriesenSpy — Token-Primitiven.

Zwei getrennte Token, beide im Format ``base64url(payload).hmac_sha256_hex``:

- Das *eingehende* SSO-Token von der Forum-Bridge ``sso.php``, signiert mit dem GETEILTEN
  ``SSO_SECRET``. Kurzlebig (``iat``-Frische ≤ 60 s), trägt einen Einmal-``nonce`` (Replay-
  Schutz erfolgt im Aufrufer).
- Das *eigene* FriesenSpy-Session-Cookie nach erfolgreichem Login, signiert mit ``SECRET_KEY``
  (analog :mod:`app.auth`), mit Ablaufzeitpunkt ``exp``.

Reine Standardbibliothek, keine zusätzliche Abhängigkeit.
"""
from __future__ import annotations

import base64
import hashlib
import hmac
import json
import time

USER_COOKIE = "fs_user"
SSO_TOKEN_MAX_AGE_SEC = 60  # Frische des eingehenden Bridge-Tokens


def _b64url_encode(raw: bytes) -> str:
    return base64.urlsafe_b64encode(raw).rstrip(b"=").decode("ascii")


def _b64url_decode(s: str) -> bytes:
    pad = "=" * (-len(s) % 4)
    return base64.urlsafe_b64decode(s + pad)


def _sign(payload_b64: str, secret: str) -> str:
    return hmac.new(secret.encode("utf-8"), payload_b64.encode("ascii"),
                    hashlib.sha256).hexdigest()


def _encode(claims: dict, secret: str) -> str:
    payload_b64 = _b64url_encode(
        json.dumps(claims, separators=(",", ":"), sort_keys=True).encode("utf-8")
    )
    return payload_b64 + "." + _sign(payload_b64, secret)


def _decode(token: str, secret: str) -> dict | None:
    if not token or not secret or token.count(".") != 1:
        return None
    payload_b64, sig = token.split(".", 1)
    if not hmac.compare_digest(sig, _sign(payload_b64, secret)):
        return None
    try:
        data = json.loads(_b64url_decode(payload_b64))
    except (ValueError, TypeError):
        return None
    return data if isinstance(data, dict) else None


def verify_sso_token(token: str, sso_secret: str, now: float | None = None) -> dict | None:
    """Eingehendes Bridge-Token prüfen: Signatur (``SSO_SECRET``) + ``iat``-Frische + ``nonce``.

    Gibt die Claims (``sub``, ``name``, ``cid``, ``is_admin``, ``iat``, ``nonce``) zurück
    oder ``None``. Der Nonce-Replay-Schutz (Einmal-Verbrauch) passiert im Aufrufer.
    """
    claims = _decode(token, sso_secret)
    if claims is None:
        return None
    iat = claims.get("iat")
    if not isinstance(iat, (int, float)):
        return None
    now = time.time() if now is None else now
    if iat > now + 5 or now - iat > SSO_TOKEN_MAX_AGE_SEC:
        return None
    if not claims.get("nonce"):
        return None
    return claims


def make_user_token(secret_key: str, sub, name: str, cid: str, is_admin: bool,
                    exp: float) -> str:
    """Eigenes FriesenSpy-Session-Cookie (signiert mit ``SECRET_KEY``), Ablauf ``exp`` (Unix-s)."""
    return _encode({"sub": sub, "name": name, "cid": cid,
                    "is_admin": bool(is_admin), "exp": int(exp)}, secret_key)


def verify_user_token(token: str, secret_key: str, now: float | None = None) -> dict | None:
    """FriesenSpy-Session-Cookie prüfen: Signatur (``SECRET_KEY``) + ``exp``. Claims oder ``None``."""
    claims = _decode(token, secret_key)
    if claims is None:
        return None
    exp = claims.get("exp")
    if not isinstance(exp, (int, float)):
        return None
    now = time.time() if now is None else now
    if now >= exp:
        return None
    return claims
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/test_forum_sso.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: Commit**

```bash
git add app/forum_sso.py tests/test_forum_sso.py
git commit -m "feat: Forum-SSO Token-Primitiven (verify_sso_token, User-Session-Token)"
```

---

### Task 2: Config + Gate-Aktiv-Helfer + Admin-Toggle-Endpoints

**Files:**
- Modify: `app/config.py:11-48` (neue Settings-Felder)
- Modify: `app/main.py` (Helfer `_forum_login_active` + Endpoints `/api/admin/forum-login`)
- Test: `tests/test_forum_sso_api.py`

**Interfaces:**
- Consumes: `get_app_setting`, `set_app_setting` (aus `app.database`), `require_admin` (aus `app.main`).
- Produces:
  - `Settings.SSO_SECRET: str`, `Settings.FORUM_SSO_URL: str`, `Settings.FORUM_SSO_CALLBACK: str`, `Settings.USER_SESSION_MAX_AGE_SEC: int`
  - `app.main._forum_login_active(conn, settings) -> bool`
  - `GET /api/admin/forum-login` → `{"enabled": bool, "configured": bool}`
  - `POST /api/admin/forum-login` (Body `{"enabled": bool}`) → `{"status": "ok", "enabled": bool}`

- [ ] **Step 1: Write the failing tests**

Create `tests/test_forum_sso_api.py`:

```python
"""Endpoint- + Gate-Tests für den Forum-SSO (Board-Login)."""
import time
from types import SimpleNamespace

import pytest
from fastapi.testclient import TestClient

from app import forum_sso, main
from app.auth import make_admin_token
from app.database import get_connection, init_db, set_app_setting

SECRET = "s3cr3t-key"
SSO = "shared-forum-secret"
PW = "test-admin-pw"
CALLBACK = "https://friesenspy.devprops.de/auth/forum/callback"
FORUM_URL = "https://board.friesenflieger.de/sso.php"


@pytest.fixture()
def env(tmp_path, monkeypatch):
    p = str(tmp_path / "t.db")
    init_db(p)
    settings = SimpleNamespace(
        DB_PATH=p, CALLSIGN_PREFIX="FRS", SECRET_KEY=SECRET, ADMIN_PASSWORD=PW,
        SSO_SECRET=SSO, FORUM_SSO_URL=FORUM_URL, FORUM_SSO_CALLBACK=CALLBACK,
        USER_SESSION_MAX_AGE_SEC=3600,
    )
    monkeypatch.setattr(main, "get_settings", lambda: settings)
    main._reset_gate_cache()  # Task 4 stellt diesen Helfer bereit
    client = TestClient(main.app)
    return SimpleNamespace(client=client, db=p, settings=settings)


def _admin_cookie() -> dict:
    return {"fs_admin": make_admin_token(SECRET, PW)}


def test_toggle_defaults_off_but_configured(env):
    r = env.client.get("/api/admin/forum-login", cookies=_admin_cookie())
    assert r.status_code == 200
    assert r.json() == {"enabled": False, "configured": True}


def test_toggle_requires_admin(env):
    assert env.client.get("/api/admin/forum-login").status_code == 401
    assert env.client.post("/api/admin/forum-login", json={"enabled": True}).status_code == 401


def test_toggle_can_be_enabled(env):
    r = env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    assert r.status_code == 200 and r.json()["enabled"] is True
    r2 = env.client.get("/api/admin/forum-login", cookies=_admin_cookie())
    assert r2.json()["enabled"] is True


def test_configured_false_without_secret(env, monkeypatch):
    env.settings.SSO_SECRET = ""
    r = env.client.get("/api/admin/forum-login", cookies=_admin_cookie())
    assert r.json() == {"enabled": False, "configured": False}
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/test_forum_sso_api.py -v`
Expected: FAIL (`AttributeError: module 'app.main' has no attribute '_reset_gate_cache'` bzw. 404 auf die Endpoints)

- [ ] **Step 3a: Add settings to `app/config.py`**

In `app/config.py`, nach dem Block `TS_REJOIN_DEBOUNCE_SEC: int = 900` (Zeile ~48), einfügen:

```python

    # Forum-SSO (Board-Login) — Bridge sso.php auf board.friesenflieger.de.
    # Alle leer/Default = Board-Login inaktiv (unabhängig vom Admin-Schalter).
    SSO_SECRET: str = ""            # GETEILT mit sso.php; niemals in git
    FORUM_SSO_URL: str = ""         # z.B. https://board.friesenflieger.de/sso.php
    FORUM_SSO_CALLBACK: str = ""    # absolute URL zu /auth/forum/callback (muss der Whitelist in sso.php entsprechen)
    USER_SESSION_MAX_AGE_SEC: int = 3600  # kurze FriesenSpy-Session → spiegelt Forum-Logout verzögert
```

- [ ] **Step 3b: Add the gate-active helper + toggle endpoints to `app/main.py`**

In `app/main.py`, direkt nach der Funktion `admin_me` (nach Zeile ~1355), einfügen:

```python
# ---------------------------------------------------------------------------
# Forum-SSO (Board-Login) — Schalter, Helfer, Endpoints
# ---------------------------------------------------------------------------

def _forum_login_active(conn, settings) -> bool:
    """True nur, wenn der Admin-Schalter AN ist UND die Bridge konfiguriert ist."""
    if not (settings.SSO_SECRET and settings.FORUM_SSO_URL and settings.FORUM_SSO_CALLBACK):
        return False
    return get_app_setting(conn, "forum_login_enabled", "0") == "1"


@app.get("/api/admin/forum-login")
async def admin_get_forum_login(request: Request):
    """Board-Login-Status für die Admin-Oberfläche."""
    require_admin(request)
    settings = get_settings()
    conn = get_connection(settings.DB_PATH)
    try:
        enabled = get_app_setting(conn, "forum_login_enabled", "0") == "1"
    finally:
        conn.close()
    configured = bool(settings.SSO_SECRET and settings.FORUM_SSO_URL and settings.FORUM_SSO_CALLBACK)
    return {"enabled": enabled, "configured": configured}


@app.post("/api/admin/forum-login")
async def admin_set_forum_login(request: Request):
    """Board-Login an-/ausschalten (Admin)."""
    require_admin(request)
    body = await request.json()
    enabled = bool(body.get("enabled"))
    conn = get_connection(get_settings().DB_PATH)
    try:
        set_app_setting(conn, "forum_login_enabled", "1" if enabled else "0")
        conn.commit()
    finally:
        conn.close()
    _reset_gate_cache()
    return {"status": "ok", "enabled": enabled}
```

(Der Helfer `_reset_gate_cache` wird in Task 4 definiert; damit die Tests dieses Tasks laufen, füge vorerst am Modulanfang von `app/main.py` — bei den anderen Modul-Konstanten — einen No-op-Platzhalter ein, den Task 4 ersetzt:)

```python
def _reset_gate_cache() -> None:
    """Platzhalter — in Task 4 durch echten Cache-Reset ersetzt."""
    return None
```

Stelle sicher, dass `get_app_setting` und `set_app_setting` importiert sind (in `app/main.py` bereits über `from app.database import ...` vorhanden — sonst ergänzen).

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/test_forum_sso_api.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add app/config.py app/main.py tests/test_forum_sso_api.py
git commit -m "feat: Board-Login Admin-Schalter (forum_login_enabled) + Konfig-Settings"
```

---

### Task 3: Login-, Callback- & Logout-Endpoints + `/api/me`

**Files:**
- Modify: `app/main.py` (Imports; Nonce-Store; `/auth/forum/login`, `/auth/forum/callback`, `/auth/forum/logout`, `/api/me`)
- Test: `tests/test_forum_sso_api.py` (weitere Tests)

**Interfaces:**
- Consumes: `forum_sso.verify_sso_token`, `forum_sso.make_user_token`, `forum_sso.verify_user_token`, `forum_sso.USER_COOKIE`, `_forum_login_active` (Task 2).
- Produces:
  - `GET /auth/forum/login` → 302 zum Forum (setzt `fs_sso_state`-Cookie) bzw. 302 `/` wenn inaktiv
  - `GET /auth/forum/callback?token=&state=` → 302 `/` (setzt `fs_user`) bzw. 400/401
  - `GET /auth/forum/logout` → 302 `/` (löscht `fs_user`)
  - `GET /api/me` → `{"logged_in": bool, "name": str, "cid": str, "is_admin": bool}`

- [ ] **Step 1: Write the failing tests**

Ergänze in `tests/test_forum_sso_api.py`:

```python
def _mint_incoming(claims: dict, secret: str = SSO) -> str:
    import base64, hashlib, hmac, json
    raw = json.dumps(claims, separators=(",", ":"), sort_keys=True).encode()
    p = base64.urlsafe_b64encode(raw).rstrip(b"=").decode()
    sig = hmac.new(secret.encode(), p.encode(), hashlib.sha256).hexdigest()
    return p + "." + sig


def test_login_redirects_to_forum_when_active(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    r = env.client.get("/auth/forum/login", follow_redirects=False)
    assert r.status_code == 302
    loc = r.headers["location"]
    assert loc.startswith(FORUM_URL)
    assert "state=" in loc and "redirect=" in loc
    assert "fs_sso_state" in r.cookies


def test_callback_sets_user_cookie(env):
    # state-Cookie setzen, das der Login gesetzt hätte
    env.client.cookies.set("fs_sso_state", "st8")
    tok = _mint_incoming({"sub": 74, "name": "Tobias", "cid": "1401925",
                          "is_admin": True, "iat": time.time(), "nonce": "n1"})
    r = env.client.get(f"/auth/forum/callback?token={tok}&state=st8", follow_redirects=False)
    assert r.status_code == 302 and r.headers["location"] == "/"
    assert "fs_user" in r.cookies
    # /api/me spiegelt den Login
    me = env.client.get("/api/me")
    assert me.json() == {"logged_in": True, "name": "Tobias", "cid": "1401925", "is_admin": True}


def test_callback_rejects_state_mismatch(env):
    env.client.cookies.set("fs_sso_state", "st8")
    tok = _mint_incoming({"iat": time.time(), "nonce": "n2", "sub": 1, "name": "x", "cid": "", "is_admin": False})
    r = env.client.get(f"/auth/forum/callback?token={tok}&state=WRONG", follow_redirects=False)
    assert r.status_code == 400


def test_callback_rejects_bad_token(env):
    env.client.cookies.set("fs_sso_state", "st8")
    r = env.client.get("/auth/forum/callback?token=garbage&state=st8", follow_redirects=False)
    assert r.status_code == 401


def test_callback_rejects_replayed_nonce(env):
    env.client.cookies.set("fs_sso_state", "st8")
    tok = _mint_incoming({"sub": 74, "name": "T", "cid": "1", "is_admin": False,
                          "iat": time.time(), "nonce": "used-once"})
    ok = env.client.get(f"/auth/forum/callback?token={tok}&state=st8", follow_redirects=False)
    assert ok.status_code == 302
    env.client.cookies.set("fs_sso_state", "st8")
    again = env.client.get(f"/auth/forum/callback?token={tok}&state=st8", follow_redirects=False)
    assert again.status_code == 401


def test_logout_clears_user_cookie(env):
    env.client.cookies.set("fs_sso_state", "st8")
    tok = _mint_incoming({"sub": 1, "name": "x", "cid": "", "is_admin": False,
                          "iat": time.time(), "nonce": "n3"})
    env.client.get(f"/auth/forum/callback?token={tok}&state=st8", follow_redirects=False)
    r = env.client.get("/auth/forum/logout", follow_redirects=False)
    assert r.status_code == 302
    assert env.client.get("/api/me").json()["logged_in"] is False
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/test_forum_sso_api.py -k "login or callback or logout or me" -v`
Expected: FAIL (404 auf die neuen Routen)

- [ ] **Step 3a: Ensure imports in `app/main.py`**

Am Kopf von `app/main.py` sicherstellen (ergänzen, falls nicht vorhanden):

```python
import secrets
from urllib.parse import quote
from fastapi.responses import RedirectResponse
from app.forum_sso import (
    USER_COOKIE, verify_sso_token, make_user_token, verify_user_token,
)
```

(`hmac` und `time` werden in `app/main.py` bereits verwendet — nicht doppelt importieren.)

- [ ] **Step 3b: Add nonce store + endpoints to `app/main.py`**

Direkt nach den Toggle-Endpoints aus Task 2 einfügen:

```python
# Einmal-Nonce-Store gegen Replay des eingehenden SSO-Tokens (In-Process, TTL = Token-Frische).
_used_sso_nonces: dict[str, float] = {}


def _consume_sso_nonce(nonce: str) -> bool:
    """True beim ERSTEN Sehen eines Nonce; False, wenn schon verbraucht. Prunt alte Einträge."""
    from app.forum_sso import SSO_TOKEN_MAX_AGE_SEC
    now = time.monotonic()
    for k, t in list(_used_sso_nonces.items()):
        if now - t > SSO_TOKEN_MAX_AGE_SEC + 5:
            _used_sso_nonces.pop(k, None)
    if not nonce or nonce in _used_sso_nonces:
        return False
    _used_sso_nonces[nonce] = now
    return True


def _is_https(request: Request) -> bool:
    return request.headers.get("x-forwarded-proto", request.url.scheme) == "https"


@app.get("/auth/forum/login")
async def forum_login(request: Request):
    """Startet den Board-Login: Redirect zur Forum-Bridge mit state + Callback."""
    settings = get_settings()
    conn = get_connection(settings.DB_PATH)
    try:
        active = _forum_login_active(conn, settings)
    finally:
        conn.close()
    if not active:
        return RedirectResponse("/", status_code=302)
    state = secrets.token_urlsafe(24)
    target = (f"{settings.FORUM_SSO_URL}?redirect={quote(settings.FORUM_SSO_CALLBACK, safe='')}"
              f"&state={quote(state, safe='')}")
    resp = RedirectResponse(target, status_code=302)
    resp.set_cookie("fs_sso_state", state, httponly=True, secure=_is_https(request),
                    samesite="lax", path="/auth/forum", max_age=300)
    return resp


@app.get("/auth/forum/callback")
async def forum_callback(request: Request):
    """Nimmt das signierte Token der Bridge entgegen → eigene FriesenSpy-Session."""
    settings = get_settings()
    token = request.query_params.get("token", "")
    state = request.query_params.get("state", "")
    cookie_state = request.cookies.get("fs_sso_state", "")
    if not state or not cookie_state or not hmac.compare_digest(state, cookie_state):
        raise HTTPException(status_code=400, detail="Ungültiger SSO-Status")
    claims = verify_sso_token(token, settings.SSO_SECRET)
    if claims is None:
        raise HTTPException(status_code=401, detail="Ungültiges SSO-Token")
    if not _consume_sso_nonce(str(claims.get("nonce", ""))):
        raise HTTPException(status_code=401, detail="SSO-Token bereits verwendet")
    exp = time.time() + settings.USER_SESSION_MAX_AGE_SEC
    user_token = make_user_token(
        settings.SECRET_KEY, claims.get("sub"), str(claims.get("name", "")),
        str(claims.get("cid", "")), bool(claims.get("is_admin")), exp,
    )
    resp = RedirectResponse("/", status_code=302)
    resp.set_cookie(USER_COOKIE, user_token, httponly=True, secure=_is_https(request),
                    samesite="lax", path="/", max_age=settings.USER_SESSION_MAX_AGE_SEC)
    resp.delete_cookie("fs_sso_state", path="/auth/forum")
    return resp


@app.get("/auth/forum/logout")
async def forum_logout():
    """Meldet NUR FriesenSpy ab (Forum-Session bleibt)."""
    resp = RedirectResponse("/", status_code=302)
    resp.delete_cookie(USER_COOKIE, path="/")
    return resp


@app.get("/api/me")
async def api_me(request: Request):
    """Login-Status für das Frontend (aus dem eigenen Session-Cookie)."""
    settings = get_settings()
    claims = verify_user_token(request.cookies.get(USER_COOKIE, ""), settings.SECRET_KEY)
    if not claims:
        return {"logged_in": False}
    return {"logged_in": True, "name": claims.get("name", ""),
            "cid": claims.get("cid", ""), "is_admin": bool(claims.get("is_admin"))}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/test_forum_sso_api.py -v`
Expected: PASS (alle, inkl. Task-2-Tests)

- [ ] **Step 5: Commit**

```bash
git add app/main.py tests/test_forum_sso_api.py
git commit -m "feat: Board-Login Endpoints (/auth/forum/login,callback,logout) + /api/me"
```

---

### Task 4: Gate-Middleware (App hinter den Login, wenn Schalter AN)

**Files:**
- Modify: `app/main.py` (Gate-Cache + `_reset_gate_cache` echt + Middleware)
- Test: `tests/test_forum_sso_api.py` (Gate-Tests)

**Interfaces:**
- Consumes: `_forum_login_active` (Task 2), `verify_user_token`/`USER_COOKIE` (Task 1), `verify_admin_token`/`ADMIN_COOKIE` (bestehend).
- Produces: `app.main._reset_gate_cache() -> None` (ersetzt den Platzhalter aus Task 2); HTTP-Middleware `forum_login_gate`.

- [ ] **Step 1: Write the failing tests**

Ergänze in `tests/test_forum_sso_api.py`:

```python
def _login(env, is_admin=False):
    env.client.cookies.set("fs_sso_state", "st8")
    tok = _mint_incoming({"sub": 9, "name": "Pilot", "cid": "1234567",
                          "is_admin": is_admin, "iat": time.time(), "nonce": f"g{time.time()}"})
    env.client.get(f"/auth/forum/callback?token={tok}&state=st8", follow_redirects=False)


def test_gate_off_allows_public_access(env):
    # Schalter AUS (Default) → App offen
    r = env.client.get("/", follow_redirects=False)
    assert r.status_code == 200


def test_gate_on_redirects_html_to_login(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    main._reset_gate_cache()
    r = env.client.get("/", headers={"accept": "text/html"}, follow_redirects=False)
    assert r.status_code == 302 and r.headers["location"] == "/auth/forum/login"


def test_gate_on_returns_401_for_api(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    main._reset_gate_cache()
    r = env.client.get("/api/teamspeak", headers={"accept": "application/json"})
    assert r.status_code == 401


def test_gate_on_allows_logged_in_user(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    main._reset_gate_cache()
    _login(env)
    assert env.client.get("/", headers={"accept": "text/html"}, follow_redirects=False).status_code == 200


def test_gate_on_allows_break_glass_admin(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    main._reset_gate_cache()
    # nur Admin-Cookie, kein fs_user
    r = env.client.get("/", headers={"accept": "text/html"},
                       cookies=_admin_cookie(), follow_redirects=False)
    assert r.status_code == 200


def test_gate_always_allows_auth_and_legal_paths(env):
    env.client.post("/api/admin/forum-login", json={"enabled": True}, cookies=_admin_cookie())
    main._reset_gate_cache()
    for path in ("/auth/forum/login", "/impressum", "/datenschutz", "/health"):
        assert env.client.get(path, follow_redirects=False).status_code in (200, 302)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pytest tests/test_forum_sso_api.py -k gate -v`
Expected: FAIL (App noch nicht gated → `/` liefert 200 statt 302 bei aktivem Schalter)

- [ ] **Step 3a: Replace the `_reset_gate_cache` placeholder in `app/main.py`**

Ersetze den Platzhalter aus Task 2 durch den echten Cache:

```python
# Gate-Aktiv-Status mit kurzem TTL cachen (Middleware läuft pro Request, auch für Assets).
_gate_cache: dict[str, float | bool] = {"val": False, "ts": 0.0}
_GATE_TTL_SEC = 5.0


def _reset_gate_cache() -> None:
    """Cache verwerfen — nach jedem Schalter-Wechsel (und in Tests) aufrufen."""
    _gate_cache["ts"] = 0.0


def _forum_login_active_cached(settings) -> bool:
    now = time.monotonic()
    if now - float(_gate_cache["ts"]) > _GATE_TTL_SEC:
        conn = get_connection(settings.DB_PATH)
        try:
            _gate_cache["val"] = _forum_login_active(conn, settings)
        finally:
            conn.close()
        _gate_cache["ts"] = now
    return bool(_gate_cache["val"])
```

- [ ] **Step 3b: Add the gate middleware to `app/main.py`**

Direkt nach der bestehenden `no_index_header`-Middleware (nach Zeile ~202) einfügen:

```python
# Pfade, die auch bei aktivem Gate IMMER erreichbar bleiben (Login-Flow, Break-glass,
# Rechtstexte, PWA-Assets, Health).
_GATE_ALLOW_PREFIXES = (
    "/auth/", "/static/", "/health", "/robots.txt", "/favicon",
    "/impressum", "/datenschutz", "/admin", "/api/admin/login",
    "/api/admin/logout", "/manifest", "/sw.js", "/api/me",
)


def _request_is_authenticated(request: Request, settings) -> bool:
    if verify_user_token(request.cookies.get(USER_COOKIE, ""), settings.SECRET_KEY):
        return True
    # Break-glass: gültiges Admin-Cookie zählt auch als eingeloggt.
    if verify_admin_token(request.cookies.get(ADMIN_COOKIE, ""),
                          settings.SECRET_KEY, settings.ADMIN_PASSWORD):
        return True
    return False


@app.middleware("http")
async def forum_login_gate(request: Request, call_next):
    path = request.url.path
    if not path.startswith(_GATE_ALLOW_PREFIXES):
        settings = get_settings()
        if _forum_login_active_cached(settings) and not _request_is_authenticated(request, settings):
            wants_html = request.method == "GET" and "text/html" in request.headers.get("accept", "")
            if wants_html:
                return RedirectResponse("/auth/forum/login", status_code=302)
            return JSONResponse({"detail": "Login erforderlich"}, status_code=401)
    return await call_next(request)
```

Stelle sicher, dass `ADMIN_COOKIE`, `verify_admin_token`, `JSONResponse` in `app/main.py` importiert sind (ADMIN_COOKIE/verify_admin_token via `from app.auth import ...` bereits vorhanden; `JSONResponse` aus `fastapi.responses` ebenfalls bereits genutzt).

- [ ] **Step 4: Run tests to verify they pass**

Run: `pytest tests/test_forum_sso_api.py -v`
Expected: PASS (alle Gate- + Endpoint-Tests)

- [ ] **Step 5: Run the full suite (keine Regression im Public-Default)**

Run: `pytest -q`
Expected: PASS — insbesondere bestehende Tests laufen weiter, weil das Gate ohne Schalter/Secrets inaktiv ist.

- [ ] **Step 6: Commit**

```bash
git add app/main.py tests/test_forum_sso_api.py
git commit -m "feat: Gate-Middleware — App hinter Board-Login wenn Schalter AN (Break-glass bleibt)"
```

---

### Task 5: Frontend — Admin-Schalter (admin.html) + Abmelden/Name (index.html)

**Files:**
- Modify: `app/static/admin.html` (Panel „Board-Login")
- Modify: `app/static/index.html` (Abmelden-Link + Name aus `/api/me`)

**Interfaces:**
- Consumes: `GET/POST /api/admin/forum-login` (Task 2), `GET /api/me`, `GET /auth/forum/logout` (Task 3).

- [ ] **Step 1: Add the admin toggle panel to `app/static/admin.html`**

Ein neues Panel im Admin-Layout ergänzen (an der Stelle der übrigen Panels, Muster analog zur Banner-/Quips-Verwaltung). Markup:

```html
<section class="panel" id="panel-forum-login">
  <h2>Board-Login</h2>
  <p class="hint">Ist der Board-Login an, ist FriesenSpy nur für eingeloggte Forum-Mitglieder
    sichtbar (Admin = Forum-Gruppe „Events"). Das Admin-Passwort bleibt als Notzugang.</p>
  <label class="row">
    <input type="checkbox" id="forumLoginToggle" />
    <span>Board-Login aktiv</span>
  </label>
  <p class="hint" id="forumLoginState">…</p>
</section>
```

JS (im bestehenden `<script>` von admin.html, wo die anderen Admin-Fetches liegen):

```javascript
async function loadForumLogin() {
  const r = await fetch('/api/admin/forum-login', { credentials: 'same-origin' });
  if (!r.ok) return;
  const d = await r.json();
  const cb = document.getElementById('forumLoginToggle');
  const st = document.getElementById('forumLoginState');
  cb.checked = d.enabled;
  cb.disabled = !d.configured;
  st.textContent = d.configured
    ? (d.enabled ? 'Aktiv — App ist login-geschützt.' : 'Aus — App ist öffentlich.')
    : 'Nicht konfiguriert (SSO_SECRET / FORUM_SSO_URL / FORUM_SSO_CALLBACK fehlen).';
}
document.getElementById('forumLoginToggle').addEventListener('change', async (e) => {
  await fetch('/api/admin/forum-login', {
    method: 'POST', credentials: 'same-origin',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ enabled: e.target.checked }),
  });
  loadForumLogin();
});
// beim Laden der Admin-Seite aufrufen (zu den bestehenden init-Aufrufen hinzufügen):
loadForumLogin();
```

- [ ] **Step 2: Add logout link + name to `app/static/index.html`**

Im Kopfbereich der SPA ein dezentes Element ergänzen (nur sichtbar, wenn eingeloggt). Der
Abmelden-Link ist klickbar → **Klickbar-Farbe** (`--green`), der Name bleibt neutral (weiß/mono)
gemäß UI-Standard:

```html
<span id="userBox" class="user-box" hidden>
  <span id="userName" class="user-name"></span>
  <a href="/auth/forum/logout" class="user-logout">Abmelden</a>
</span>
```

```css
.user-box { display: inline-flex; gap: 8px; align-items: center; font-size: .85rem; }
.user-name { color: #fff; font-family: ui-monospace, monospace; }   /* neutrale Anzeige */
.user-logout { color: var(--green); }                                /* klickbar = blau */
```

```javascript
fetch('/api/me', { credentials: 'same-origin' })
  .then(r => r.json())
  .then(d => {
    if (!d.logged_in) return;
    document.getElementById('userName').textContent = d.name || ('CID ' + (d.cid || '—'));
    document.getElementById('userBox').hidden = false;
  })
  .catch(() => {});
```

- [ ] **Step 3: Manual verification**

Run: `uvicorn app.main:app --reload` (mit `SSO_SECRET`/`FORUM_SSO_URL`/`FORUM_SSO_CALLBACK` in `config.env`)
- `/admin` → einloggen → Panel „Board-Login" sichtbar, Schalter spiegelt Status, Umschalten persistiert (Reload zeigt denselben Zustand).
- Schalter AUS → `/` lädt normal ohne Login.
- Schalter AN, nicht eingeloggt → `/` leitet auf `/auth/forum/login` (→ würde zum Forum gehen; ohne echte Bridge endet es an der Forum-URL — erwartetes Verhalten).
- Mit gültigem Admin-Cookie (Break-glass) → `/` lädt trotz aktivem Schalter.

Expected: Verhalten wie beschrieben; keine JS-Fehler in der Konsole.

- [ ] **Step 4: Commit**

```bash
git add app/static/admin.html app/static/index.html
git commit -m "feat: Board-Login UI — Admin-Schalter + Abmelden/Name in der SPA"
```

---

### Task 6: Fertige Bridge-Datei `sso.php` zum Kopieren (Forum wird NICHT angefasst)

**Files:**
- Create: `deploy/forum/sso.php`
- Create: `deploy/forum/sso_config.sample.php`
- Create: `deploy/forum/README.md`

**Interfaces:**
- Produces (Vertrag mit FriesenSpy): Redirect auf `FORUM_SSO_CALLBACK?token=<base64url(payload)>.<hmac_sha256_hex>&state=<state>` mit Payload `{sub,name,cid,is_admin,iat,nonce}`, signiert mit `SSO_SECRET` — exakt das Format, das `forum_sso.verify_sso_token` (Task 1) erwartet.

- [ ] **Step 1: Create `deploy/forum/sso_config.sample.php`**

```php
<?php
// Vorlage — echte Datei heißt sso_config.php, liegt NEBEN sso.php und ist NICHT in git.
return [
    // Muss identisch zu SSO_SECRET in FriesenSpys config.env sein (langer Zufallsstring):
    'sso_secret'   => 'HIER-LANGES-GEHEIMNIS-EINSETZEN',
    // Erlaubtes Rücksprung-Ziel (muss FORUM_SSO_CALLBACK in FriesenSpy entsprechen):
    'callback'     => 'https://friesenspy.devprops.de/auth/forum/callback',
    // Interner phpBB-Feldname des VATSIM-CID-Profilfelds „VatSim-ID"
    // (per `SELECT field_ident FROM phpbb_profile_fields;` bestätigen):
    'cid_field'    => 'pf_vatsim_id',
    // phpBB-Gruppen-ID, die FriesenSpy-Admin ergibt (Gruppe „Events"):
    'admin_gid'    => 8,
];
```

- [ ] **Step 2: Create `deploy/forum/sso.php`**

```php
<?php
/**
 * FriesenSpy Board-Login-Bridge für phpBB 3.3.
 * Liegt im Forum-Docroot (/var/www/bb_friesen/). Liest NUR die phpBB-Session,
 * das Profilfeld und die Gruppen — schreibt nie ins Forum. Löschen = Zustand wie vorher.
 */
define('IN_PHPBB', true);
$phpbb_root_path = __DIR__ . '/';
$phpEx = 'php';
include($phpbb_root_path . 'common.' . $phpEx);
include($phpbb_root_path . 'includes/functions_user.' . $phpEx);

$cfg = require __DIR__ . '/sso_config.php';

$user->session_begin();
$auth->acl($user->data);
$user->setup();

// Rücksprung-Ziel gegen Whitelist prüfen.
$redirect = isset($_GET['redirect']) ? (string) $_GET['redirect'] : '';
$state    = isset($_GET['state']) ? (string) $_GET['state'] : '';
if (!hash_equals($cfg['callback'], $redirect)) {
    http_response_code(400);
    exit('bad redirect');
}

// Nicht eingeloggt → phpBBs eigene Login-Seite, danach zurück auf diese URL.
if ((int) $user->data['user_id'] === ANONYMOUS) {
    login_box(build_url(array('redirect')) . '&redirect=' . urlencode($_SERVER['REQUEST_URI']));
    exit;
}

$uid = (int) $user->data['user_id'];

// VATSIM-CID aus dem Profilfeld lesen.
$cid = '';
try {
    $pf   = $phpbb_container->get('profilefields.manager');
    $data = $pf->grab_profile_fields_data($uid);
    if (isset($data[$uid][$cfg['cid_field']]['value'])) {
        $cid = (string) $data[$uid][$cfg['cid_field']]['value'];
    }
} catch (\Exception $e) {
    $cid = '';
}

// Admin = Mitglied der Gruppe „Events".
$is_admin = !empty(group_memberships((int) $cfg['admin_gid'], $uid, false));

// Signiertes Kurz-Token (base64url(payload).hmac) bauen.
$payload = array(
    'sub'      => $uid,
    'name'     => (string) $user->data['username'],
    'cid'      => $cid,
    'is_admin' => (bool) $is_admin,
    'iat'      => time(),
    'nonce'    => bin2hex(random_bytes(16)),
);
ksort($payload);
$json = json_encode($payload, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE);
$payload_b64 = rtrim(strtr(base64_encode($json), '+/', '-_'), '=');
$sig = hash_hmac('sha256', $payload_b64, $cfg['sso_secret']);

header('Location: ' . $cfg['callback'] . '?token=' . $payload_b64 . '.' . $sig
    . '&state=' . rawurlencode($state));
exit;
```

> **Hinweis zur Signatur-Kompatibilität:** FriesenSpy verifiziert über `base64url(payload_b64)` als
> HMAC-Eingabe. `sso.php` signiert denselben `payload_b64`-String — Formate stimmen überein.
> Die JSON-Serialisierung muss auf beiden Seiten NICHT byte-identisch sein, weil FriesenSpy den
> empfangenen `payload_b64` selbst dekodiert und die Signatur über GENAU diesen String prüft
> (nicht über eine neu serialisierte Fassung).

- [ ] **Step 3: Create `deploy/forum/README.md`**

```markdown
# FriesenSpy Board-Login-Bridge (`sso.php`)

Diese Datei macht den phpBB-Forum-Login als SSO für FriesenSpy nutzbar. Sie wird **neben
phpBB** abgelegt und **verändert das Forum nicht** (kein Patch, keine Extension, nur Lesen).

## Installation (durch den Forum-Admin)

1. `sso.php` in den Forum-Docroot kopieren: `/var/www/bb_friesen/sso.php`.
2. `sso_config.sample.php` → `sso_config.php` daneben kopieren und ausfüllen:
   - `sso_secret`: langer Zufallsstring, **identisch** zu `SSO_SECRET` in FriesenSpys `config.env`.
   - `callback`: `https://friesenspy.devprops.de/auth/forum/callback`.
   - `cid_field`: interner Feldname des Profilfelds „VatSim-ID“
     (bestätigen mit `SELECT field_ident FROM phpbb_profile_fields;`).
   - `admin_gid`: `8` (Gruppe „Events“).
3. Testen: `https://board.friesenflieger.de/sso.php?redirect=https://friesenspy.devprops.de/auth/forum/callback&state=test`
   → als eingeloggtes Forum-Mitglied leitet es mit `?token=…&state=test` zurück.
4. In FriesenSpy: Admin → **Board-Login** einschalten.

## Entfernen

`sso.php` (und `sso_config.php`) löschen → Forum ist exakt wie vorher. In FriesenSpy den
Schalter ausschalten.
```

- [ ] **Step 4: Syntax-Check (optional, wenn PHP lokal vorhanden)**

Run: `php -l deploy/forum/sso.php`
Expected: `No syntax errors detected` — falls kein PHP lokal installiert ist, überspringen; die Datei wird ohnehin auf dem Forum-Server (PHP 8.4) ausgeführt.

- [ ] **Step 5: Commit**

```bash
git add deploy/forum/
git commit -m "feat: fertige Board-Login-Bridge sso.php (zum Kopieren, Forum unangetastet)"
```

---

### Task 7: Doku, config.env-Beispiel, Version-Bump & Changelog

**Files:**
- Modify: `README.md`
- Modify: `docs/api.md`
- Modify: `docs/architecture.md`
- Modify: `config.env`-Beispiel im README / `CLAUDE.md`-Konfig-Abschnitt (Board-Login-Variablen)
- Modify: `app/CHANGELOG.json`

- [ ] **Step 1: Document the new config in README.md**

Im Konfig-Abschnitt der `README.md` (und im `config.env`-Beispiel) ergänzen:

```bash
# Forum-SSO (Board-Login) — optional; alle leer = Board-Login inaktiv
SSO_SECRET=                # GETEILT mit sso.php auf dem Forum; niemals in git
FORUM_SSO_URL=https://board.friesenflieger.de/sso.php
FORUM_SSO_CALLBACK=https://friesenspy.devprops.de/auth/forum/callback
USER_SESSION_MAX_AGE_SEC=3600   # kurze FriesenSpy-Session (spiegelt Forum-Logout verzögert)
```

Zusätzlich einen kurzen Absatz: „Board-Login wird im Admin-Tab an-/ausgeschaltet
(`forum_login_enabled`), Default AUS; Admin-Rechte = Forum-Gruppe „Events“; das
`ADMIN_PASSWORD` bleibt als Break-glass.“

- [ ] **Step 2: Document endpoints in docs/api.md**

Neue Endpoints eintragen: `GET /auth/forum/login`, `GET /auth/forum/callback`,
`GET /auth/forum/logout`, `GET /api/me`, `GET/POST /api/admin/forum-login`. Je eine Zeile mit
Zweck, Auth-Anforderung und Rückgabe (analog zum bestehenden Stil in `docs/api.md`).

- [ ] **Step 3: Document architecture in docs/architecture.md**

Abschnitt „Forum-SSO (Board-Login)“ ergänzen: Redirect-Flow (Bridge → Callback → Session),
Gate-Middleware + Allowlist, Break-glass, die Dateien `app/forum_sso.py` und `deploy/forum/sso.php`.
Auf die Spec `docs/superpowers/specs/2026-07-13-forum-sso-design.md` verweisen.

- [ ] **Step 4: Version bump + changelog entry**

In `app/CHANGELOG.json` als **obersten** Eintrag hinzufügen (Minor-Feature):

```json
{
  "version": "8.23.0",
  "date": "2026-07-13",
  "title": "Board-Login (Forum-SSO) — einschaltbar",
  "highlight": true,
  "notes": "FriesenSpy kann per Admin-Schalter hinter den Forum-Login gestellt werden: nur Mitglieder, Identifikation über die VATSIM-CID aus dem Forum-Profil, Admin = Gruppe „Events“. Default AUS, Admin-Passwort bleibt als Notzugang. Forum wird nicht verändert (eine kopierbare Bridge-Datei)."
}
```

Prüfen, ob die Version an weiterer Stelle gepflegt wird (z. B. `app/config.py`- oder
`main.py`-Versionskonstante); falls vorhanden, ebenfalls auf `8.23.0` heben.

- [ ] **Step 5: Run the full suite once more**

Run: `pytest -q`
Expected: PASS (keine Regression).

- [ ] **Step 6: Commit + Tag**

```bash
git add README.md docs/api.md docs/architecture.md app/CHANGELOG.json
git commit -m "docs: Board-Login (Forum-SSO) dokumentiert + v8.23.0"
git tag v8.23.0
```

(Push auf `main` erst nach kurzer Bestätigung durch den Nutzer — stehende Regel.)

---

## Self-Review

**1. Spec coverage:**
- Ziel „Zugangsschranke“ → Task 4 (Gate-Middleware). ✓
- „Identifikation über CID“ → Task 1 (`cid` im Token) + Task 3 (`/api/me`). ✓
- „Admin-Ablösung (Gruppe Events)“ → Task 6 (`admin_gid=8` in `sso.php`) + `is_admin` durchgereicht. ✓
- „Kernanforderung: Passwort nie durch FriesenSpy“ → Login passiert in `sso.php`/phpBB; FriesenSpy sieht nur Token. ✓
- „Admin-Schalter, eine Datei kopieren“ → Task 2 (Toggle) + Task 6 (Bridge). ✓
- Token/Sicherheit (HMAC, iat≤60 s, Nonce, state, HTTPS, Whitelist) → Task 1 + Task 3. ✓
- „Break-glass Admin-Passwort behalten“ → Task 4 (`_request_is_authenticated`). ✓
- „Session an Forum koppeln“ → kurze `USER_SESSION_MAX_AGE_SEC` + stilles Nachvalidieren beim Ablauf (Gate leitet erneut zu `/auth/forum/login`). ✓ (sofortiges Spiegeln bewusst nicht, siehe Spec §6.2)
- „Forum nicht anfassen“ → Task 6 liefert Datei nur ins Repo; Global Constraints. ✓

**2. Placeholder scan:** Der einzige bewusste Platzhalter ist `_reset_gate_cache` in Task 2, der in Task 4 durch die echte Implementierung ersetzt wird (explizit vermerkt). Kein „TODO/TBD“, alle Code-Schritte enthalten vollständigen Code.

**3. Type consistency:** `verify_sso_token`/`make_user_token`/`verify_user_token`/`USER_COOKIE` identisch zwischen Task 1 (Definition) und Task 3/4 (Nutzung). Token-Format `base64url(payload).hmac_hex` identisch zwischen Task 1 (Python) und Task 6 (PHP). `forum_login_enabled` (String `"0"/"1"`) konsistent zwischen Task 2 (Schreiben) und Task 4 (Lesen). `_reset_gate_cache` in Task 2 (Platzhalter) → Task 4 (echt) konsistent benannt.
